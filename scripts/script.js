// Obtendo a largura e altura do viewport
var larguraViewport = window.innerWidth;
var alturaViewport = window.innerHeight;

console.log("Largura do viewport: " + larguraViewport + "px");
console.log("Altura do viewport: " + alturaViewport + "px");
